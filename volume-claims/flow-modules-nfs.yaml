apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: flow-modules-nfs
spec:
    accessModes:
        - ReadWriteMany
    storageClassName: "flow-modules-nfs"
    resources:
        requests:
            storage: 1Gi