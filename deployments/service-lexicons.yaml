apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-lexicons
spec:
    selector:
        matchLabels:
            app: service-lexicons
    replicas: 1
    template:
        metadata:
            labels:
                app: service-lexicons
        spec:
            containers:
            - name: service-lexicons
              image: dev-docker.cognigy.com:5000/service-lexicons:c599ffe0
              resources:
                requests:
                    memory: "60Mi"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: LEXICONS_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-lexicons
                        key: db-password
              - name: RABBITMQ_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-rabbitmq
                        key: rabbitmq-password
            imagePullSecrets:
            - name: cognigy-registry-token