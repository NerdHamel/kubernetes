apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-cleanup
spec:
    selector:
        matchLabels:
            app: service-cleanup
    replicas: 1
    template:
        metadata:
            labels:
                app: service-cleanup
        spec:
            containers:
            - name: service-cleanup
              image: dev-docker.cognigy.com:5000/service-cleanup:c599ffe0
              resources:
                limits:
                    memory: "200Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              env:
              - name: RABBITMQ_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-rabbitmq
                        key: rabbitmq-password
              envFrom:
              - configMapRef:
                    name: cognigy-env
            imagePullSecrets:
            - name: cognigy-registry-token