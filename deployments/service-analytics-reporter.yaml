apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-analytics-reporter
spec:
    selector:
        matchLabels:
            app: service-analytics-reporter
    replicas: 1
    template:
        metadata:
            labels:
                app: service-analytics-reporter
        spec:
            containers:
            - name: service-analytics-reporter
              image: dev-docker.cognigy.com:5000/service-analytics-reporter:c599ffe0
              resources:
                limits:
                    memory: "512Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: ANALYTICS_REPORTER_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-analytics-collector-provider
                        key: db-password
              - name: RABBITMQ_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-rabbitmq
                        key: rabbitmq-password
            imagePullSecrets:
            - name: cognigy-registry-token