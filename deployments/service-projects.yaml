apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-projects
spec:
    selector:
        matchLabels:
            app: service-projects
    replicas: 1
    template:
        metadata:
            labels:
                app: service-projects
        spec:
            containers:
            - name: service-projects
              image: dev-docker.cognigy.com:5000/service-projects:c599ffe0
              resources:
                limits:
                    memory: "512Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: PROJECTS_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-projects
                        key: db-password
              - name: RABBITMQ_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-rabbitmq
                        key: rabbitmq-password
            imagePullSecrets:
            - name: cognigy-registry-token