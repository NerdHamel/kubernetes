apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-analytics-conversation-provider
spec:
    selector:
        matchLabels:
            app: service-analytics-conversation-provider
    replicas: 1
    template:
        metadata:
            labels:
                app: service-analytics-conversation-provider
        spec:
            containers:
            - name: service-analytics-conversation-provider
              image: dev-docker.cognigy.com:5000/service-analytics-conversation-provider:c599ffe0
              resources:
                requests:
                    memory: "60Mi"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: CONVERSATION_PROVIDER_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-analytics-conversation-collector-provider
                        key: db-password
              - name: RABBITMQ_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-rabbitmq
                        key: rabbitmq-password
            imagePullSecrets:
            - name: cognigy-registry-token