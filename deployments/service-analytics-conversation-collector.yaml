apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-analytics-conversation-collector
spec:
    selector:
        matchLabels:
            app: service-analytics-conversation-collector
    replicas: 1
    template:
        metadata:
            labels:
                app: service-analytics-conversation-collector
        spec:
            containers:
            - name: service-analytics-conversation-collector
              image: dev-docker.cognigy.com:5000/service-analytics-conversation-collector:c599ffe0
              resources:
                requests:
                    memory: "60Mi"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: CONVERSATION_COLLECTOR_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-analytics-conversation-collector-provider
                        key: db-password
              - name: RABBITMQ_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-rabbitmq
                        key: rabbitmq-password
            imagePullSecrets:
            - name: cognigy-registry-token