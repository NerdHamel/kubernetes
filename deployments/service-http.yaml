apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-http
spec:
    selector:
        matchLabels:
            app: service-http
    replicas: 1
    template:
        metadata:
            labels:
                app: service-http
        spec:
            containers:
            - name: service-http
              image: dev-docker.cognigy.com:5000/service-http:c599ffe0
              resources:
                limits:
                    memory: "512Mi"
                requests:
                    memory: "60Mi"
              env:
              - name: RABBITMQ_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-rabbitmq
                        key: rabbitmq-password
              envFrom:
              - configMapRef:
                    name: cognigy-env
            imagePullSecrets:
            - name: cognigy-registry-token