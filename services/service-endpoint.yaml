apiVersion: v1
kind: Service
metadata:
    name: service-endpoint
    labels:
        app: service-endpoint
spec:
    ports:
    - name: service-endpoint
      port: 80
      targetPort: 80
      protocol: TCP
    selector:
        app: service-endpoint