apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-nlp-score-en
spec:
    selector:
        matchLabels:
            app: service-nlp-score-en
    replicas: 1
    template:
        metadata:
            labels:
                app: service-nlp-score-en
        spec:
            containers:
            - name: service-nlp-score-en
              image: docker.cognigy.com:5000/service-nlp:54d71e0a299e35157f59496112e145bf7b9cf37d
              resources:
                limits:
                    memory: "1Gi"
                    cpu: "500m"
                requests:
                    memory: "500Mi"
                    cpu: "500m"
              # mount persistent volume claim for nlp config and models
              volumeMounts:
              - name: nlp-models-nfs
                mountPath: /data/models
              - name: languages-config
                mountPath: /languages
              - name: rabbitmq-connection-string
                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets/rabbitmqConnectionString
                subPath: rabbitmqConnectionString
              - name: mongodb-connection-string
                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets/mongodbConnectionString
                subPath: mongodbConnectionString
              envFrom:
                - configMapRef:
                    name: cognigy-env
              env:
              - name: LANGUAGES_TO_RUN
                value: en-US
              - name: SCORE
                value: "true"
              - name: LANGUAGE_CONFIG_PATH
                value: /languages/languages.json
            volumes:
            - name: nlp-models-nfs
              persistentVolumeClaim:
                claimName: nlp-models-nfs
            - name: languages-config
              configMap:
                name: languages-config
            - name: rabbitmq-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string
                    path: rabbitmqConnectionString
            - name: mongodb-connection-string
              secret:
                secretName: cognigy-service-nlp
                items:
                  - key: connection-string
                    path: mongodbConnectionString
            imagePullSecrets:
            - name: cognigy-registry-token