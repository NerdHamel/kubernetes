apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-nlp-train-nl
spec:
  selector:
    matchLabels:
      app: service-nlp-train-nl
  replicas: 1
  template:
    metadata:
      labels:
        app: service-nlp-train-nl
    spec:
      containers:
        - name: service-nlp-train-nl
          image: 'docker.cognigy.com:5000/service-nlp:072b74166ec5c78cfce4768ce772dfe8a8f6864c'
          resources:
            requests:
              cpu: '0.350'
              memory: 960M
            limits:
              cpu: '0.5'
              memory: 1200M
          volumeMounts:
            - name: nlp-models-nfs
              mountPath: /data/models
            - name: languages-config
              mountPath: /languages
            - name: rabbitmq-connection-string
              mountPath: /var/run/secrets/rabbitmqConnectionString
              subPath: rabbitmqConnectionString
            - name: mongodb-connection-string
              mountPath: /var/run/secrets/mongodbConnectionString
              subPath: mongodbConnectionString
            - name: local
              mountPath: /data/local
          livenessProbe:
            exec:
              command:
                - python
                - '-u'
                - health_check.py
            failureThreshold: 1
            initialDelaySeconds: 900
            periodSeconds: 60
            timeoutSeconds: 15
          envFrom:
            - configMapRef:
                name: cognigy-env
          env:
            - name: LANGUAGES_TO_RUN
              value: nl-NL
            - name: TRAIN
              value: 'true'
            - name: LANGUAGE_CONFIG_PATH
              value: /languages/languages.json
            - name: NLP_REDIS_LOCK
              value: 'false'
      volumes:
        - name: nlp-models-nfs
          persistentVolumeClaim:
            claimName: nlp-models-nfs
        - name: languages-config
          configMap:
            name: languages-config
        - name: rabbitmq-connection-string
          secret:
            secretName: cognigy-rabbitmq
            items:
              - key: connection-string
                path: rabbitmqConnectionString
        - name: mongodb-connection-string
          secret:
            secretName: cognigy-service-nlp
            items:
              - key: connection-string
                path: mongodbConnectionString
        - name: local
          emptyDir: {}
      imagePullSecrets:
        - name: cognigy-registry-token
