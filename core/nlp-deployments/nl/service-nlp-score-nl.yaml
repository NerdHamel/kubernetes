apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-nlp-score-nl
spec:
  selector:
    matchLabels:
      app: service-nlp-score-nl
  replicas: 1
  template:
    metadata:
      labels:
        app: service-nlp-score-nl
    spec:
      containers:
        - name: service-nlp-score-nl
          image: 'docker.cognigy.com:5000/service-nlp:06e10ebd59e4b0afaecc707869a30657d548af6d'
          resources:
            requests:
              cpu: '0.350'
              memory: 960M
            limits:
              cpu: '0.5'
              memory: 1200M
          volumeMounts:
            - name: nlp-models-nfs
              mountPath: /data/models
            - name: languages-config
              mountPath: /languages
            - name: rabbitmq-connection-string
              mountPath: /var/run/secrets/rabbitmqConnectionString
              subPath: rabbitmqConnectionString
            - name: mongodb-connection-string
              mountPath: /var/run/secrets/mongodbConnectionString
              subPath: mongodbConnectionString
            - name: local
              mountPath: /data/local
          livenessProbe:
            exec:
              command:
                - python
                - '-u'
                - health_check.py
            failureThreshold: 1
            initialDelaySeconds: 900
            periodSeconds: 60
            timeoutSeconds: 15
          envFrom:
            - configMapRef:
                name: cognigy-env
          env:
            - name: LANGUAGES_TO_RUN
              value: nl-NL
            - name: SCORE
              value: 'true'
            - name: LANGUAGE_CONFIG_PATH
              value: /languages/languages.json
            - name: NLP_REDIS_LOCK
              value: 'false'
      volumes:
        - name: nlp-models-nfs
          persistentVolumeClaim:
            claimName: nlp-models-nfs
        - name: languages-config
          configMap:
            name: languages-config
        - name: rabbitmq-connection-string
          secret:
            secretName: cognigy-rabbitmq
            items:
              - key: connection-string
                path: rabbitmqConnectionString
        - name: mongodb-connection-string
          secret:
            secretName: cognigy-service-nlp
            items:
              - key: connection-string
                path: mongodbConnectionString
        - name: local
          emptyDir: {}
      imagePullSecrets:
        - name: cognigy-registry-token
