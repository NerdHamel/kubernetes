apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-persistent
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: redis-persistent
  resources:
    requests:
      storage: 10Gi
