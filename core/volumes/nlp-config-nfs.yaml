apiVersion: v1
kind: PersistentVolume
metadata:
    name: nlp-config-nfs
spec:
    capacity:
        storage: 5Mi
    accessModes:
        - ReadWriteMany
    storageClassName: "nlp-config-nfs"
    nfs:
        server: "127.0.0.1"
        path: "/var/opt/cognigy/data/config"