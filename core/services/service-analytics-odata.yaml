apiVersion: v1
kind: Service
metadata:
    name: service-analytics-odata
    labels:
        app: service-analytics-odata
spec:
    ports:
    - name: service-analytics-odata
      port: 80
      targetPort: 80
      protocol: TCP
    selector:
        app: service-analytics-odata