apiVersion: v1
kind: Service
metadata:
    name: service-endpoint
    labels:
        app: service-endpoint
spec:
    ports:
    - name: service-endpoint
      port: 8000
      targetPort: 8000
      protocol: TCP
    selector:
        app: service-endpoint