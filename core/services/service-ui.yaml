apiVersion: v1
kind: Service
metadata:
    name: service-ui
    labels:
        app: service-ui
spec:
    ports:
    - name: service-ui
      port: 9000
      targetPort: 9000
      protocol: TCP
    selector:
        app: service-ui