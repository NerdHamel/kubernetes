apiVersion: v1
kind: Service
metadata:
  name: service-api
  labels:
    app: service-api
spec:
  ports:
  - name: service-api
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: service-api