apiVersion: v1
kind: Secret
metadata:
    name: cognigy-mongodb-exporter
type: Opaque
data:
    # The full mongodb connection string, has a schema of:
    # mongodb://cognigy-mongodb-exporter:<password>@<host>:<port>/admin
    connection-string: