apiVersion: v1
kind: Secret
metadata:
    name: cognigy-redis-persistent-password
type: Opaque
data:
   redis-persistent-password.conf: |-

    # You need to replace the password and base64 encode the following line and remove the comments.
    # requirepass someUniquePassword