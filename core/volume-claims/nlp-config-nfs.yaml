apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: nlp-config-nfs
spec:
    accessModes:
        - ReadWriteMany
    storageClassName: "nlp-config-nfs"
    resources:
        requests:
            storage: 5Mi