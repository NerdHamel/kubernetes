apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: nlp-models-nfs
spec:
    accessModes:
        - ReadWriteMany
    storageClassName: "nlp-models-nfs"
    resources:
        requests:
            storage: 15Gi