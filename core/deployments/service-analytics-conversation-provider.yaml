apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-analytics-conversation-provider
spec:
    selector:
        matchLabels:
            app: service-analytics-conversation-provider
    replicas: 1
    template:
        metadata:
            labels:
                app: service-analytics-conversation-provider
        spec:
            containers:
            - name: service-analytics-conversation-provider
              image: docker.cognigy.com:5000/service-analytics-conversation-provider:5d0ed4a5
              resources:
                limits:
                    memory: "200Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: CONVERSATION_PROVIDER_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-analytics-conversation-collector-provider
                        key: db-password
              volumeMounts:
              - name: rabbitmq-connection-string

                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets
              livenessProbe:
                exec:
                  command:
                  - node
                  - /opt/healthcheck.js
                initialDelaySeconds: 30
                periodSeconds: 30
                failureThreshold: 1
                timeoutSeconds: 2

            volumes:
            - name: rabbitmq-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string
                    path: rabbitmqConnectionString
            imagePullSecrets:
            - name: cognigy-registry-token