apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-ui
spec:
    selector:
        matchLabels:
            app: service-ui
    replicas: 1
    template:
        metadata:
            labels:
                app: service-ui
        spec:
            containers:
            - name: service-ui
              image: docker.cognigy.com:5000/service-ui:5d0ed4a5
              resources:
                limits:
                    memory: "200Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
            imagePullSecrets:
            - name: cognigy-registry-token
