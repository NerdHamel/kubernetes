# This file was generated by the 'cutils' tool. Timestamp: 2019-09-03T16:19:11.483Z
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-projects
spec:
  selector:
    matchLabels:
      app: service-projects
  replicas: 1
  template:
    metadata:
      labels:
        app: service-projects
    spec:
      containers:
        - name: service-projects
          image: 'office-docker.cognigy.com:5000/service-projects:18594edbf49c390040764d20fe8132bb5070d2cc'
          resources:
            requests:
              cpu: '0.1'
              memory: 60M
            limits:
              cpu: '0.1'
              memory: 75M
          envFrom:
            - configMapRef:
                name: cognigy-env
          volumeMounts:
            - name: rabbitmq-connection-string
              mountPath: /var/run/secrets/rabbitmqConnectionString
              subPath: rabbitmqConnectionString
            - name: mongodb-connection-string
              mountPath: /var/run/secrets/mongodbConnectionString
              subPath: mongodbConnectionString
          livenessProbe:
            exec:
              command:
                - node
                - /opt/healthcheck.js
            failureThreshold: 3
            initialDelaySeconds: 90
            periodSeconds: 60
            timeoutSeconds: 10
      volumes:
        - name: rabbitmq-connection-string
          secret:
            secretName: cognigy-rabbitmq
            items:
              - key: connection-string
                path: rabbitmqConnectionString
        - name: mongodb-connection-string
          secret:
            secretName: cognigy-service-projects
            items:
              - key: connection-string
                path: mongodbConnectionString
      imagePullSecrets:
        - name: cognigy-registry-token
