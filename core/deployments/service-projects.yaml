apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-projects
spec:
    selector:
        matchLabels:
            app: service-projects
    replicas: 1
    template:
        metadata:
            labels:
                app: service-projects
        spec:
            containers:
            - name: service-projects
              image: docker.cognigy.com:5000/service-projects:5d0ed4a5
              resources:
                limits:
                    memory: "512Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: PROJECTS_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-projects
                        key: db-password
              volumeMounts:
              - name: rabbitmq-connection-string

                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets

            volumes:
            - name: rabbitmq-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string
                    path: rabbitmqConnectionString
            imagePullSecrets:
            - name: cognigy-registry-token