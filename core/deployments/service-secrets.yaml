apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-secrets
spec:
    selector:
        matchLabels:
            app: service-secrets
    replicas: 1
    template:
        metadata:
            labels:
                app: service-secrets
        spec:
            containers:
            - name: service-secrets
              image: docker.cognigy.com:5000/service-secrets:5d0ed4a5
              resources:
                requests:
                    memory: "50Mi"
                    cpu: "46m"
                limits:
                    memory: "75Mi"
                    cpu: "46m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: SECRETS_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-secrets
                        key: db-password
              volumeMounts:
              - name: rabbitmq-connection-string

                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets
              livenessProbe:
                exec:
                  command:
                  - node
                  - /opt/healthcheck.js
                initialDelaySeconds: 30
                periodSeconds: 30
                failureThreshold: 1
                timeoutSeconds: 2

            volumes:
            - name: rabbitmq-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string
                    path: rabbitmqConnectionString
            imagePullSecrets:
            - name: cognigy-registry-token

