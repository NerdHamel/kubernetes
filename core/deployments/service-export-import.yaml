apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-export-import
spec:
    selector:
        matchLabels:
            app: service-export-import
    replicas: 1
    template:
        metadata:
            labels:
                app: service-export-import
        spec:
            containers:
            - name: service-export-import
              image: docker.cognigy.com:5000/service-export-import:59ab503ee3c18e32533c65f66ee559c3a19ca99d
              resources:
                requests:
                    memory: "300Mi"
                    cpu: "46m"
                limits:
                    memory: "450Mi"
                    cpu: "46m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              volumeMounts:
              - name: rabbitmq-connection-string
                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets
              livenessProbe:
                exec:
                  command:
                  - node
                  - /opt/healthcheck.js
                initialDelaySeconds: 30
                periodSeconds: 30
                failureThreshold: 1
                timeoutSeconds: 2
            volumes:
            - name: rabbitmq-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string
                    path: rabbitmqConnectionString
            imagePullSecrets:
            - name: cognigy-registry-token