apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-analytics-conversation-collector
spec:
    selector:
        matchLabels:
            app: service-analytics-conversation-collector
    replicas: 1
    template:
        metadata:
            labels:
                app: service-analytics-conversation-collector
        spec:
            containers:
            - name: service-analytics-conversation-collector
              image: docker.cognigy.com:5000/service-analytics-conversation-collector:27e9d3c2375437d52f632c6555cdddbfc0b48831
              resources:
                requests:
                    memory: "150Mi"
                    cpu: "46m"
                limits:
                    memory: "225Mi"
                    cpu: "46m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              # env:
              # - name: CONVERSATION_COLLECTOR_DB_PASSWORD
              #   valueFrom:
              #       secretKeyRef:
              #           name: cognigy-service-analytics-conversation-collector-provider
              #           key: db-password
              volumeMounts:
              - name: rabbitmq-connection-string
                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets/rabbitmqConnectionString
                subPath: rabbitmqConnectionString
              - name: mongodb-connection-string
                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets/mongodbConnectionString
                subPath: mongodbConnectionString
              livenessProbe:
                exec:
                  command:
                  - node
                  - /opt/healthcheck.js
                initialDelaySeconds: 30
                periodSeconds: 30
                failureThreshold: 1
                timeoutSeconds: 2

            volumes:
            - name: rabbitmq-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string
                    path: rabbitmqConnectionString
            - name: mongodb-connection-string
              secret:
                secretName: cognigy-service-analytics-conversation-collector-provider
                items:
                  - key: connection-string
                    path: mongodbConnectionString
            imagePullSecrets:
            - name: cognigy-registry-token