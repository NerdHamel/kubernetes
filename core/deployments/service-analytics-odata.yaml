apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-analytics-odata
spec:
    selector:
        matchLabels:
            app: service-analytics-odata
    replicas: 1
    template:
        metadata:
            labels:
                app: service-analytics-odata
        spec:
            containers:
            - name: service-analytics-odata
              image: docker.cognigy.com:5000/service-analytics-odata:5d0ed4a5
              resources:
                limits:
                    memory: "512Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: ANALYTICS_COLLECTOR_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-analytics-collector-provider
                        key: db-password
              - name: ODATA_SUPER_API_KEY
                valueFrom:
                    secretKeyRef:
                        name: cognigy-odata
                        key: odata-super-api-key
              - name: SECURITY_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-security
                        key: db-password
              - name: FLOWS_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-flows
                        key: db-password
              volumeMounts:
              - name: rabbitmq-connection-string

                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets
              livenessProbe:
                exec:
                  command:
                  - node
                  - /opt/healthcheck.js
                initialDelaySeconds: 30
                periodSeconds: 30
                failureThreshold: 1
                timeoutSeconds: 2

            volumes:
            - name: rabbitmq-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string
                    path: rabbitmqConnectionString
            imagePullSecrets:
            - name: cognigy-registry-token