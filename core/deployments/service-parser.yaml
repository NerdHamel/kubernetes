apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-parser
spec:
    selector:
        matchLabels:
            app: service-parser
    replicas: 1
    template:
        metadata:
            labels:
                app: service-parser
        spec:
            containers:
            - name: service-parser
              image: dev-docker.cognigy.com:5000/service-parser:c599ffe0
              resources:
                limits:
                    memory: "512Mi"
                    cpu: "50m"
                requests:
                    memory: "200Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: LEXICONS_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-lexicons
                        key: db-password
              - name: RABBITMQ_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-rabbitmq
                        key: rabbitmq-password
            imagePullSecrets:
            - name: cognigy-registry-token