apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-settings
spec:
    selector:
        matchLabels:
            app: service-settings
    replicas: 1
    template:
        metadata:
            labels:
                app: service-settings
        spec:
            containers:
            - name: service-settings
              image: dev-docker.cognigy.com:5000/service-settings:c599ffe0
              resources:
                limits:
                    memory: "200Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: SETTINGS_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-settings
                        key: db-password
              - name: RABBITMQ_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-rabbitmq
                        key: rabbitmq-password
            imagePullSecrets:
            - name: cognigy-registry-token