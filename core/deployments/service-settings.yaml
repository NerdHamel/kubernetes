apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-settings
spec:
    selector:
        matchLabels:
            app: service-settings
    replicas: 1
    template:
        metadata:
            labels:
                app: service-settings
        spec:
            containers:
            - name: service-settings
              image: docker.cognigy.com:5000/service-settings:5d0ed4a5
              resources:
                limits:
                    memory: "200Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: SETTINGS_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-settings
                        key: db-password
              volumeMounts:
              - name: rabbitmq-connection-string

                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets

            volumes:
            - name: rabbitmq-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string
                    path: rabbitmqConnectionString
            imagePullSecrets:
            - name: cognigy-registry-token