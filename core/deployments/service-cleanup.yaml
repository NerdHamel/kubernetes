apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-cleanup
spec:
    selector:
        matchLabels:
            app: service-cleanup
    replicas: 1
    template:
        metadata:
            labels:
                app: service-cleanup
        spec:
            containers:
            - name: service-cleanup
              image: docker.cognigy.com:5000/service-cleanup:193513c655ce7f3c2d125667a7d6320eb7f17ab5
              resources:
                requests:
                    memory: "50Mi"
                    cpu: "46m"
                limits:
                    memory: "75Mi"
                    cpu: "46m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              volumeMounts:
              - name: rabbitmq-connection-string
                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets/rabbitmqConnectionString
                subPath: rabbitmqConnectionString
              - name: rabbitmq-api-connection-string
                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets/rabbitmqAPIConnectionString
                subPath: rabbitmqAPIConnectionString
              livenessProbe:
                exec:
                  command:
                  - node
                  - /opt/healthcheck.js
                initialDelaySeconds: 30
                periodSeconds: 30
                failureThreshold: 1
                timeoutSeconds: 2
            volumes:
            - name: rabbitmq-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string
                    path: rabbitmqConnectionString
            - name: rabbitmq-api-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string-api
                    path: rabbitmqAPIConnectionString
            imagePullSecrets:
            - name: cognigy-registry-token