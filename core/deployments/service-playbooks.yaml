apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-playbooks
spec:
    selector:
        matchLabels:
            app: service-playbooks
    replicas: 1
    template:
        metadata:
            labels:
                app: service-playbooks
        spec:
            containers:
            - name: service-playbooks
              image: dev-docker.cognigy.com:5000/service-playbooks:c599ffe0
              resources:
                limits:
                    memory: "200Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: PLAYBOOKS_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-playbooks
                        key: db-password
              - name: RABBITMQ_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-rabbitmq
                        key: rabbitmq-password
            imagePullSecrets:
            - name: cognigy-registry-token