apiVersion: apps/v1
kind: Deployment
metadata:
    name: service-nlp-connectors
spec:
    selector:
        matchLabels:
            app: service-nlp-connectors
    replicas: 1
    template:
        metadata:
            labels:
                app: service-nlp-connectors
        spec:
            containers:
            - name: service-nlp-connectors
              image: docker.cognigy.com:5000/service-nlp-connectors:5d0ed4a5
              resources:
                limits:
                    memory: "200Mi"
                    cpu: "50m"
                requests:
                    memory: "100Mi"
                    cpu: "20m"
              envFrom:
              - configMapRef:
                    name: cognigy-env
              env:
              - name: NLP_CONNECTORS_DB_PASSWORD
                valueFrom:
                    secretKeyRef:
                        name: cognigy-service-nlp-connectors
                        key: db-password
              volumeMounts:
              - name: rabbitmq-connection-string

                # This will mount the file in /run/secrets. Alpine Linux bug
                mountPath: /var/run/secrets

            volumes:
            - name: rabbitmq-connection-string
              secret:
                secretName: cognigy-rabbitmq
                items:
                  - key: connection-string
                    path: rabbitmqConnectionString
            imagePullSecrets:
            - name: cognigy-registry-token